Global:
// ACHTUNG! Haupt-Gruppen müssen von RECHTS nach LINKS eingeteilt werden
// Hundred, thousand, million - wird erst hinzugefügt, wenn die einzelnen Gruppen erledigt sind
// Fälle die noch nicht funktionieren:  
// erledigte Fälle: 505, 580, 005, 30, 1000000000, -1000000000, 500000000

function makeGroups(text)
// Hat lange gedauert: Ich wollte die Zähl-Schleife auf decrement abändern und hab lange nach Fehlern gesucht, anstatt es einfach neu zu bauen.

function convertSubGroup(group)
// Wie starte ich die Wort-Ausgabe, anhängig von der Länge des
// group-Arrays? 
// LÖSUNG: Array-Länge abfragen, abhängig davon hunderter/10er-Prüfung weglassen

General:
// Musste unshift verwenden, damit die Einträge vor eingefügt und von links nach recht in dreier Gruppen unterteil werden.
// Die Erstellung der Nummern-Gruppen und das "Drucken" der einzelnen Wörter habe ich getrennt erstellt. Beim Verbinden dieser Blöcke hatte ich Schwierigkeiten.
// Wie stelle ich in "convertGroupsToWords" fest, welche "digitGrouping" ich brauche?
// arraylength = group.length; convertSubGroup ERROR: group is undefined. Solved -> changed case 1 to convertSubGroup(groups[0]) in convertGroupsToWords
// Bei Millionenbeträgen ohne tausender wie z.b. 500000000 wird ein unnötiges "thousand" angestellt.


Die Zähl-Schleife in Make-Groups() auf Decrement ändern. Wäre leichter gewesen, wenn ich die Schleife neu erstellt hätte.
Die Reihenfolge beim Erstellen der Gruppen von Rechts nach Links geändert. Damit unvollständige Gruppen möglichst bei den größeren Stellen wie thousand und million sind.








JS-Kurs:

Muss "this" in node.js auch stabilisiert werden?
wird "use-strict" in der Praxis verwendet? 
Template string tags?
warum ist NaN !== NaN
Immediate Invoked Function (IIFE) - Geschützter Gültigkeitsbereich, vermeiden von Globalen Variablen, UseCase?
Closures - Wenn innherhalb einer Funktion eine Funktion zurückgegeben wird, kann in der inneren Funktion auf den äußeren Gültigkeitsbereich zugegriffen?
